apiVersion: apps/v1
kind: Deployment  
metadata:
  name: {{ .Values.rentcarApp.appName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.rentcarApp.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.rentcarApp.appName }}
    spec:
      containers:
        - name: {{ .Values.rentcarApp.appName }}
          image: "{{ .Values.rentcarApp.image.name }}:{{ .Values.rentcarApp.image.tag }}"
          ports:
            - containerPort: {{ .Values.rentcarApp.port }}
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: db_host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: db_port
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: db_name
      imagePullSecrets:
        - name: registrypullsecret
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.rentcarApp.appName }}
spec:
  selector:
    app: {{ .Values.rentcarApp.appName }}
  ports:
    - port: 80
      targetPort: {{ .Values.rentcarApp.port }}
